import { NavLink } from "react-router";

const Header = () => {
  return (
    <div>
      <NavLink to="">Pomodoros</NavLink>
      <NavLink to="">About</NavLink>
    </div>
  );
};

export default Header;
